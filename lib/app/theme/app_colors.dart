import 'package:flutter/material.dart';

/// Seed colors driving [ColorScheme.fromSeed] for light/dark themes.
class AppColors {
  const AppColors._();

  static const Color seed = Color(0xFF2E7D5B);

  static const Color expense = Color(0xFFD9534F);
  static const Color income = Color(0xFF2E7D5B);
  static const Color warning = Color(0xFFE0A800);
  static const Color danger = Color(0xFFC62828);
}
