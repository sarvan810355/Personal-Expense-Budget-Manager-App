/// Local notification scheduling for budget alerts (80/90/100% thresholds)
/// and recurring expense reminders. Wired up in Phase 4 (Budget) and
/// Phase 7 (Advanced) once `flutter_local_notifications` is added — this
/// Phase 1 stub only reserves the service's place in the architecture.
class NotificationService {
  const NotificationService();

  Future<void> initialize() async {}
}
