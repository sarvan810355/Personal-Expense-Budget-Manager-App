/// CSV/Excel/PDF export of expenses and income. Implemented in Phase 7
/// once the full data model is finalized (§9) — this Phase 1 stub only
/// reserves the service's place in the architecture.
class ExportService {
  const ExportService();
}
